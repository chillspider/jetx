export enum AuthProvider {
	email = 'email',
	google = 'google',
	apple = 'apple',
}

export enum UserStatus {
	ACTIVE = 'active',
	INACTIVE = 'inactive',
	SUSPENDED = 'suspended',
	BANNED = 'banned',
}
