export enum NotificationType {
  ORDER = 'order',
  SUPPORT = 'support',
  INFO = 'info',
  CAMPAIGN = 'campaign',
}

export enum NotificationProcess {
  ALL = 'all',
  CUSTOMER = 'customer',
  CAMPAIGN = 'campaign',
}

export enum NotificationTarget {
  ALL = 'all',
  SPECIFIC = 'specific',
}
