export enum Routes {
	Home = "/",
	Login = "/login",
	CarDetector = "/car-detector",
}

export const publicRoutes: string[] = [Routes.Login];
export const authRoutes: string[] = [Routes.Login];
